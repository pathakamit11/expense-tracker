Login with 3 hardcoded users (admin/1234, staff/1234, student/1234) + error handling
Dashboard with 3 stat cards (Total, This Month, Average), 6-month bar chart, full expenses table with category filter & delete
Add Expense form with name, amount, category (7 types), date, note + recent entries preview
LocalStorage — each user's data is stored separately
Logout button in sidebar
Dark glassmorphism UI with teal accents, smooth animations, toast notifications
